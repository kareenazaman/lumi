<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

  <!-- Bubble color (sent) -->
  <solid android:color="#00332F" />

  <!-- Rounded corners -->
  <corners android:radius="18dp" />

  <!-- Inner padding -->
  <padding
      android:left="10dp"
      android:top="6dp"
      android:right="10dp"
      android:bottom="6dp" />
</shape>
